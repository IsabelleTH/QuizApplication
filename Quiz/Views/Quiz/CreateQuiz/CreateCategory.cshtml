@model Quiz.Models.CategoryModel
@{
    ViewBag.Title = "";
}

<h2>Create A New Quiz</h2>

<ul>This is the first step in creating a new quiz for the users!</ul>
<ul>First, type in a category in which the quiz will be. </ul>


<div>
    <a href=@Url.Action("CreateQuestions") %>
        <button class="btn btn-default">Programming</button>
    </a>
</div>

<hr />

<div>
    <a href=@Url.Action("CreateQuestions") %>
        <button class="btn btn-default">Computers</button>
    </a>
</div>

@*@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />
        @Html.HiddenFor(m => m.Id)

        <div class="form-group">
            @Html.LabelFor(m => m.Category, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-8">
                @Html.DropDownListFor(m => m.Category,
     new List<SelectListItem>
     {
                    
                    new SelectListItem { Value = "Programming", Text= "Programming"},
                    new SelectListItem { Value = "Computers", Text= "Computers"},

     },
     new { @class= "col-md-10"})
           
            
            </div>
        </div>

    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Continue" class="btn btn-primary" />
        </div>
    </div>
}*@